.primal
  .primal__top
    STOCK-APP 商品取消

  .items-table
    .items-table__head
      商品一覧
    %table
      %tbody
        %tr
          %th.name
            商品名
          %th.stock
            在庫数
          %th.del
            取消

      - @stocks.each do |stock|
        %tr
          %td
            =stock.name
          %td
            =stock.count
          %td
            =link_to "取消",stock_path(stock.id), method: :delete, data: { confirm: '削除してよろしいですか?' }
      
    .back
      = link_to "戻る", root_path,class: "back__btn"


